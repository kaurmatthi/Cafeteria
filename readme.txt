To start the application, use "docker-compose build" and then "docker-compose up -d" on the root folder

User 1, secret = "secret1"
User 2, secret = "secret2"

Application can be accessed from http://localhost/      (port 80)

API documentation (swagger):  http://localhost:7292/swagger/index.html
